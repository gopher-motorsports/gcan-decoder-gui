[README]
; please note, the gui does batch error checking for each of these groups / section
; if one of the configs settings in a group is invalid / missing the entire group / module will default to their default values
; run make_config.py to remake this file with the recommended settings for aquaman / tempest
; TODO: edit this file for SAE port

[Global]
init_window_width = 1200
init_window_height = 900
window_min_height = 700
window_min_width = 700

[Footer]
default_vid = 1155
default_pid = 14155
default_comm_bus = GopherCAN
default_device_addr = N\A
default_comm_timer = 1000
usb_baud_rate = 9600

[Parameter]
default_gcan_file = go4-23c.yaml

